type Radius = {
  xs: number;
  sm: number;
  md: number;
  lg: number;
  xl: number;
  full: number;
};

export const radius: Radius = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  full: 9999,
};
