// stubs/react-native-google-mobile-ads.js
//
// v1.0 ad-free placeholder for the uninstalled `react-native-google-mobile-ads`
// package (ads deferred to v1.1). Aliased in metro.config.js so the production
// bundle resolves. Never executed at runtime — lib/ads.ts only require()s this
// when ADS_ENABLED is true, which is false in v1.0. To re-enable ads in v1.1:
// `npx expo install react-native-google-mobile-ads` and remove the Metro alias.
module.exports = {};
